version: '3.8'

services:
  teamio-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: teamio-web
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
